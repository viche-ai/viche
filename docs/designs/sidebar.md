# Sidebar Navigation Design & Changes

This document outlines the changes required for the global sidebar navigation menu present across the application.

## 1. Remove "Demo" Section
The sidebar currently includes a section header labeled "Demo" containing two navigation links. This entire block should be removed.
*   **Action:** Delete the "Demo" section header.
*   **Action:** Delete the `Network Demo` navigation link.
*   **Action:** Delete the `Join` navigation link.

## 2. Retain
*   **Main Navigation:** The core navigation links (Dashboard, Network, Agents, Inboxes) are approved and remain as-is.
*   **Bottom Section:** The footer section of the sidebar containing the `Settings` and `GitHub` links is approved and must remain intact at the bottom of the menu.