{
  "mcpServers": {
    "viche": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/viche-server.ts"],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_DATA}/node_modules"
      }
    }
  }
}
